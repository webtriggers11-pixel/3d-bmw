# CODEBASE — 3d-bmw
> 📥 Generated by vibe-init on 2026-06-28.
> All entries observed from actual codebase.

> ℹ️ Fresh `create-next-app` scaffold — only the default landing page exists.
> This map will grow as real features are added.

---

## 1. Tech stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Runtime | Node.js | not pinned (no `.nvmrc`/engines) |
| Framework | Next.js (App Router) | 16.2.9 |
| UI library | React | 19.2.4 |
| Language | TypeScript | ^5 (strict) |
| Styling | Tailwind CSS | ^4 |
| Linting | ESLint | ^9 (flat config) |
| Testing | none | — |
| Database / ORM | none | — |

---

## 2. Commands

```bash
# Development
npm run dev      # next dev

# Build
npm run build    # next build

# Start (production)
npm run start    # next start

# Lint
npm run lint     # eslint

# Test
# Not observed — no test runner configured

# Database
# Not applicable — no database
```

---

## 3. Folder map

| Folder | Purpose |
|--------|---------|
| `app/` | App Router root — routes, layouts, global styles |
| `public/` | Static assets (SVG logos) served from root |

---

## 4. Key files

| File | Purpose | Depended on by |
|------|---------|---------------|
| `app/layout.tsx` | Root layout — `<html>`/`<body>` shell, Geist fonts, global CSS import | All routes |
| `app/page.tsx` | Home route (`/`) — default create-next-app landing page | Router (`/`) |
| `app/globals.css` | Tailwind v4 import + CSS theme tokens (light/dark) | `app/layout.tsx` |
| `next.config.ts` | Next.js config (currently empty) | Next.js build |
| `tsconfig.json` | TS strict config, `@/*` → root path alias | TS compiler |
| `eslint.config.mjs` | ESLint flat config (next core-web-vitals + typescript) | `npm run lint` |
| `postcss.config.mjs` | PostCSS → `@tailwindcss/postcss` | Tailwind build |
| `AGENTS.md` | Agent rules — warns Next.js 16 has breaking changes; read `node_modules/next/dist/docs/` | Every agent session (via `CLAUDE.md` import) |

---

## 5. Routes / API endpoints

### App Router pages
| Method | Path | Handler | Auth | Notes |
|--------|------|---------|------|-------|
| GET | `/` | `Home` (`app/page.tsx`) | no | Default landing page |

> ⚠️ From vibe-init — the live app has many `app/api/*` routes (donations, razorpay
> webhook, cars, leaderboard, admin). New this feature:
> | GET | `/api/placement/preview?amountPaise=<int>` | preview route | no | Read-only; returns the candidate placement (zone/anchor/scale) or `blocked`+`upgradeHint`. `force-dynamic`, rate-limited 30/60s. |

---

## 6. Data model

No data model observed — no schema files, ORM, or database present.

---

## 7. Key components

### `RootLayout`
- **File:** `app/layout.tsx`
- **Purpose:** App-wide HTML shell; loads Geist & Geist Mono fonts as CSS variables, imports global styles, sets `<html>`/`<body>` classes (`h-full antialiased`, `min-h-full flex flex-col`).
- **Props:** `children: React.ReactNode`
- **Used by:** Next.js App Router (wraps all routes)
- **Notes:** Exports `metadata` (title "Create Next App" — default, not yet customized).

### `Home`
- **File:** `app/page.tsx`
- **Purpose:** Default create-next-app landing page — Next.js logo, "edit page.tsx" prompt, Deploy/Docs links.
- **Props:** none
- **Used by:** route `/`
- **Notes:** Placeholder content; uses `next/image` and Tailwind utility classes with dark-mode variants.

---

## 8. Services / modules

> ⚠️ This section is from vibe-init and under-describes the live donation app.
> Placement business logic (added 2026-06-28):
> - `lib/sizing.ts` — contribution (paise) → size tier (XS–XXL) + render `SIZE_SCALE`.
> - `lib/placement.ts` — **placement source of truth**: maps the 12 anchors to value
>   zones (Premium/Standard/Economy), size→eligible-zone rules, prominence ordering,
>   and the upgrade-nudge text. Client-safe (no env/Prisma). Consumed by
>   `server/positions.ts` (allocation), the preview endpoint, and the donation form.
> - `server/positions.ts#reserveFreePosition(carId, size)` — now takes the paid
>   `size` and reserves the best free anchor in an eligible zone (was: alphabetical).
> - `server/placement.ts#pickCandidate(carId, size)` — read-only twin used by the
>   preview endpoint; never reserves.
> - Client glue: `store/useDonationModal.ts` holds the live `draft`;
>   `features/donate/usePlacementPreview.ts` queries the preview endpoint;
>   `features/donate/DonationModal.tsx` is a drawer; `features/viewer/PreviewName.tsx`
>   renders the ghost name inside CarViewer's Canvas while the drawer is open.

---

## 9. Integration points

No external integrations observed. No `.env.example`; no environment variables required yet.

---

## 10. Patterns

### Import pattern
Absolute alias `@/*` available (maps to root); current files use package + relative imports.
```tsx
// app/layout.tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
```

### Component pattern
Default-exported function components, typed props inline.
```tsx
// app/layout.tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
```

### Styling pattern
Tailwind v4 utility classes inline in JSX, with `dark:` variants and CSS custom properties.
```css
/* app/globals.css */
@import "tailwindcss";
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
}
```

### API call pattern
No API call pattern observed — no data fetching exists yet.

### Error handling pattern
No error-handling pattern observed — no `error.tsx`/try-catch present.

### Test pattern
No test patterns observed — no test suite found during init.

---

## Changelog

| Date | Change |
|------|--------|
| 2026-06-28 | Generated by vibe-init — 12 source files read |
