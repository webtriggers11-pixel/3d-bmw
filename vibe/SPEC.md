# SPEC — 3d-bmw
> ⚠️ PROVISIONAL SPEC — generated by vibe-init on 2026-06-28 from observed code behaviour.
> This describes what the code DOES, not necessarily what it was INTENDED to do.
> Inferred features may encode bugs as requirements. Verify before using vibe-review.
> Last verified: unverified — pending human review
> To clear this banner: verify each feature below, correct any errors, then update
> "Last verified" to today's date and remove this warning block.

---

## Overview

`3d-bmw` is a **fresh Next.js 16 scaffold** with no custom application logic yet —
only the default `create-next-app` landing page is implemented. The project name
suggests an intended **3D BMW visualization / configurator**, but no 3D rendering
code, models, or related libraries are present in the codebase at init time.

[Inferred from: `README.md`, `app/page.tsx`, `app/layout.tsx`, `package.json` name field]

**Apparent users:** not determinable — no auth, routes, or UI beyond the default page.
**Apparent core value:** not yet implemented — intent inferred from project name only.

---

## Observed features

### Default landing page
[Inferred from: `app/page.tsx`, `app/layout.tsx`, `app/globals.css`]

**What the code does:**
- Serves a single page at `/` showing the Next.js logo, a "To get started, edit the page.tsx file" heading, and links to Vercel Templates / Next.js Learning.
- Provides "Deploy Now" and "Documentation" call-to-action links to external Vercel/Next.js URLs.
- Renders a responsive layout with light/dark mode via `prefers-color-scheme` and Tailwind `dark:` variants.
- Loads Geist and Geist Mono fonts through `next/font/google`.

**Acceptance criteria (inferred):**
- [ ] App boots with `npm run dev` and renders the landing page at `http://localhost:3000`
- [ ] Page is responsive and respects system light/dark preference
- [ ] No console/build errors with `npm run build`

> ⚠️ This is boilerplate, not a real product feature. The actual `3d-bmw`
> requirements are **not determined during init** and must be defined by the team.

---

## Data model

No data model observed — no schema, ORM, or database present.

---

## Tech stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Framework | Next.js (App Router) | 16.2.9 |
| UI library | React | 19.2.4 |
| Language | TypeScript | ^5 (strict) |
| Styling | Tailwind CSS | ^4 |
| Linting | ESLint | ^9 |
| Testing | none | — |

---

## Integrations

No external services integrated. The landing page links out to Vercel/Next.js
documentation URLs, but these are static hyperlinks, not integrations.

---

## Out of scope

> Not determined during init — scope boundaries unknown without original requirements.
> Update this section as the team clarifies what is and isn't in scope.
>
> Open question: is this meant to be a 3D BMW visualization/configurator? If so,
> no 3D stack (three.js / react-three-fiber / model assets) exists yet.

---

## Conformance checklist

> To be defined by the team after verifying the provisional spec.
> Run `vibe-review` once to establish a baseline — findings will inform this list.

---

## Changelog

| Date | Change |
|------|--------|
| 2026-06-28 | Generated by vibe-init — provisional |
