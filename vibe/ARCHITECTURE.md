# ARCHITECTURE — 3d-bmw
> 📥 Generated by vibe-init on 2026-06-28 from codebase analysis.
> Reflects patterns observed in the actual code — not planned decisions.
> Update this file as new patterns are deliberately introduced.
> Every agent session reads this file before writing code.

> ℹ️ This project is a **fresh `create-next-app` scaffold**. Only the default
> landing page exists. Patterns below are the framework defaults — there is no
> custom application architecture yet. Treat this as the baseline to build on.

---

## Project type

Full-stack-capable web app (currently frontend-only).
Framework: **Next.js 16.2.9** (App Router) · React 19.2.4 · TypeScript 5.

> ⚠️ Next.js 16 has breaking changes vs. earlier versions. Per `AGENTS.md`,
> read the relevant guide in `node_modules/next/dist/docs/` before writing code —
> do not assume APIs match older Next.js knowledge.

---

## Architecture overview

```
┌─────────────────────────────────────────────┐
│              Client (Next.js 16)             │
│   app/layout.tsx  ·  app/page.tsx            │
│   App Router · React Server Components        │
│   Tailwind CSS v4 (app/globals.css)           │
└───────────────────────┬─────────────────────┘
                        │ (no API layer yet)
┌───────────────────────▼─────────────────────┐
│         No backend / DB / API routes         │
│         observed during init                 │
└─────────────────────────────────────────────┘
```

[Observed in: `app/layout.tsx`, `app/page.tsx`, `package.json`, folder structure]

---

## Folder structure

```
3d-bmw/
├── app/                 ← App Router root — routes, layouts, global styles [observed]
│   ├── layout.tsx       ← Root layout, fonts, <html>/<body> shell [observed]
│   ├── page.tsx         ← Home route ("/") — default landing page [observed]
│   ├── globals.css      ← Tailwind v4 import + CSS theme tokens [observed]
│   └── favicon.ico      ← App icon [observed]
├── public/              ← Static assets served at root (svg logos) [observed]
├── next.config.ts       ← Next.js config (currently empty) [observed]
├── tsconfig.json        ← TypeScript config, strict mode, @/* path alias [observed]
├── eslint.config.mjs    ← ESLint flat config (next + typescript) [observed]
├── postcss.config.mjs   ← PostCSS → @tailwindcss/postcss plugin [observed]
└── package.json         ← Scripts + dependencies [observed]
```

**Folder rules inferred from observed patterns:**
- Routes live in `app/` as folders/files following App Router conventions [Observed in: `app/page.tsx`, `app/layout.tsx`]
- Static assets live in `public/` and are referenced from root, e.g. `src="/next.svg"` [Observed in: `app/page.tsx`]
- The `@/*` import alias maps to the project root [Observed in: `tsconfig.json` paths]
- No `components/`, `lib/`, `hooks/`, or `api/` folders exist yet — create them as the app grows [Observed: absent in topology scan]

---

## Naming conventions

- **Files:** lowercase for route files — e.g. `page.tsx`, `layout.tsx`, `globals.css` (App Router convention)
- **Components:** PascalCase — e.g. `Home`, `RootLayout` (default exports)
- **Functions:** camelCase — none beyond components observed yet
- **Variables:** camelCase — e.g. `geistSans`, `geistMono`, `nextConfig`
- **Constants:** camelCase for config objects — e.g. `eslintConfig`, `config`
- **CSS:** Tailwind utility classes inline in JSX + CSS custom properties (`--background`, `--foreground`)
- **API routes:** not determined during init — no routes exist

[Observed in: `app/layout.tsx`, `app/page.tsx`, `next.config.ts`, `eslint.config.mjs`]

---

## State management

**Approach:** none observed.

State management approach not determined during init — no client state, context,
or store libraries are present. Observe and document as the codebase is worked on.

---

## Error handling

**Approach:** none observed.

No `error.tsx`, `not-found.tsx`, try/catch blocks, or error boundaries exist yet.
Next.js App Router supports `error.tsx` / `global-error.tsx` convention files —
adopt these as the standard error-handling pattern when building real features.

---

## Backend architecture

No backend observed. No API routes (`app/api/`), route handlers, server actions,
database, or ORM are present. This is a frontend-only scaffold at init time.

---

## Testing

**Test runner:** none found.
**Test location:** not found.
**Coverage:** not configured.

**Commands:**
```bash
# No test command in package.json scripts
```

**Coverage observed:**
- Unit tests: absent
- Integration tests: absent
- E2E tests: absent

No test suite found during init. Adding tests is recommended before running
vibe-review — findings will include this as P1.

---

## Code quality

**Linter:** ESLint 9 (flat config).
**Formatter:** none found (no Prettier config).
**TypeScript:** strict mode enabled.

**Observed lint config:**
- Extends `eslint-config-next/core-web-vitals`
- Extends `eslint-config-next/typescript`
- Global ignores: `.next/**`, `out/**`, `build/**`, `next-env.d.ts`

**Commands:**
```bash
npm run lint   # eslint
```

---

## Dependencies

**Package manager:** npm
**Lock file:** present (`package-lock.json`)
**Audit:** run `npm audit` before committing dependency changes.

**Key dependencies (from manifest):**
| Package | Version | Purpose |
|---------|---------|---------|
| next | 16.2.9 | React framework (App Router) |
| react | 19.2.4 | UI library |
| react-dom | 19.2.4 | React DOM renderer |
| tailwindcss | ^4 | Utility-first CSS |
| @tailwindcss/postcss | ^4 | Tailwind PostCSS plugin |
| typescript | ^5 | Type system |
| eslint | ^9 | Linter |
| eslint-config-next | 16.2.9 | Next.js lint rules |

---

## Tech stack

| Layer | Technology | Version | Notes |
|-------|-----------|---------|-------|
| Runtime | Node.js | not pinned | no `.nvmrc` / `engines` field |
| Framework | Next.js | 16.2.9 | App Router; breaking changes vs older versions |
| UI library | React | 19.2.4 | |
| Language | TypeScript | ^5 | strict mode |
| Styling | Tailwind CSS | ^4 | via `@tailwindcss/postcss` |
| Fonts | next/font (Geist, Geist Mono) | — | self-hosted Google fonts |
| Linting | ESLint | ^9 | flat config |
| Database | none | — | not present |
| Deployment | Vercel (implied) | — | inferred from README + create-next-app defaults |

---

## Known gaps from init

- [ ] **Project intent** — name `3d-bmw` suggests a 3D BMW visualization, but no 3D
      library (three.js, react-three-fiber, etc.) or models are present. Confirm intent with team.
- [ ] **Node version** — no `.nvmrc` or `engines` field; confirm target runtime with team.
- [ ] **Deployment environment and CI/CD pipeline** — README implies Vercel; confirm with team.
- [ ] **Environment variables** — no `.env.example` present; none required yet.
- [ ] **Testing strategy** — no test runner configured; decide before first review.
